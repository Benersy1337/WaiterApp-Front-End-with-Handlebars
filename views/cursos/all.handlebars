<div class="container-cadastro">
  <div class="board">
    <a href="/cursos/create" class="button btn btn-success">Novo Pedido</a>
    <br />
    <a name="cursos"></a>
    <div class="row justify-content-center">
      {{#each cursos}}
        <div class="board-pedidos">
          <div class="card">
            <img
              class="img-pedido"
              src="../img/products/{{this.imagem}}"
              class="card-img-top"
              alt="..."
            />
            <div class="card-body">
              <h5 class="card-title">Mesa {{this.nome}}</h5>
              <p class="card-text">Pedido {{this.resumo}}</p>
              <div class="actions">
                <a href="/cursos/{{this._id}}" class="button-modal">Detalhes</a>
                <a
                  href="/cursos/edit/{{this._id}}"
                  class="button-modal"
                >Editar</a>
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
</div>